version: 2

builds:
  - goos:
      - windows
    goarch:
      - amd64
    ldflags:
      - -s -w
      - -H windowsgui
    flags:
      - -trimpath

archives:
  - formats: [binary]
    name_template: "{{ .ProjectName }}_{{ .Version }}_windows_amd64"

release:
  github:
    owner: BananaJeanss
    name: gta-nosave-go
